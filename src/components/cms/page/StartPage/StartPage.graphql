fragment StartPageData on StartPage {
  _metadata {
    key
    version
    published
    lastModified
    displayName
    url {
      base
      default
      hierarchical
    }
  }
  Heading
  MainIntro
  MainContentArea {
    __typename
    _metadata {
      key
      displayName
      published
      url {
        base
        default
        hierarchical
      }
    }
  }
}
